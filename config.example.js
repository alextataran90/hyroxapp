/**
 * Hyrox Trainer — local config (TEMPLATE)
 *
 * Copy this file to config.js, fill in your values.
 * config.js is gitignored and never committed to the repo.
 *
 * On GitHub Pages (where config.js doesn't exist) the app falls back
 * to whatever is saved in Settings → Nutrition inside the app.
 */
window.HYROX_CONFIG = {
  // Get a free key at: https://aistudio.google.com
  // Free tier: 1,500 photo analyses per day on Gemini 2.0 Flash
  geminiKey: "AIzaSy_PASTE_YOUR_KEY_HERE"
};
